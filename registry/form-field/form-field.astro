---
import { cls } from '@/src/lib/utils';

interface Props {
  class?: string;
  legend: string;
}

const { class: className, legend } = Astro.props as Props;
---

<fieldset class={cls('space-y-3 py-2', className)}>
  <legend class="sr-only">{legend}</legend>
  <slot />
</fieldset>
