---
import { cls } from '@/src/lib/utils';

interface Props {
  class?: string;
}

const { class: className } = Astro.props as Props;
---

<nav
  aria-label="Paination"
  class={cls(
    'flex items-center flex-col sm:flex-row  justify-between gap-4 mt-8 not-prose w-full py-2',
    className
  )}
>
  <slot />
</nav>
