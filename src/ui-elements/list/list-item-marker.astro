---
import { cls } from "@/src/lib/utils";

interface Props {
    class?:string;
}

const  { class: className } = Astro.props as Props
---

<div class={cls("shrink-0 mt-[0.35em] max-w-3 max-h-3", className)}>
  <slot />
</div>