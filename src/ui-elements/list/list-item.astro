---
import { cls } from "@/src/lib/utils";

interface Props {
    class?:string;
}

const  { class: className } = Astro.props as Props
---

<li class={cls("text-foreground flex items-start gap-3",className)}>
    <slot />
</li>