---
import { cls } from '@/src/lib/utils';

interface Props {
  class?: string;
}

const { class: className } = Astro.props as Props;
---

<li class={cls('text-[0.8rem] opacity-50 relative top-0 mb-2', className)}>
  <slot />
</li>
