---
import { cls } from '@/src/lib/utils';

interface Props {
  class?: string;
}

const { class: className } = Astro.props as Props;
---

<div data-slot="tabs-panel" class={cls('flex gap-3 relative w-full', className)}>
  <slot />
</div>
